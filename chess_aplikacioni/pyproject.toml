[project]
name = "chess"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "arcade",
    "chess-core",
    "requests>=2.34.2",
    "websockets>=17.0.1",
]

[tool.uv.sources]
chess-core = { path = "../chess-core", editable = true }
